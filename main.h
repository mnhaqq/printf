#ifndef MAIN_H
#define MAIN_H

/* Include files */
#include <stdio.h>
#include <stdarg.h>
#include <stdlib.h>
#include <unistd.h>

/* Prototype */
int _printf(const char *format, ...);
int _putchar(char c);

#endif /* MAIN_H */
